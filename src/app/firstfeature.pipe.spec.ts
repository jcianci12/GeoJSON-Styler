import { FirstfeaturePipe } from './firstfeature.pipe';

describe('FirstfeaturePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstfeaturePipe();
    expect(pipe).toBeTruthy();
  });
});
